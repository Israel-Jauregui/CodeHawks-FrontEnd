export default function TeamModule() {
  return (
    <section className="team-module about-section" id="team">
      <fieldset>
        <legend>Team Workspace</legend>
        <p className="lead">
          Team features are being modularized next. This view is now loaded inside the browser window shell so navigation remains seamless.
        </p>
      </fieldset>
    </section>
  );
}
